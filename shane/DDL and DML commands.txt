CREATE DATABASE bsis3a;

CREATE TABLE students (
	StudentID INT PRIMARY KEY,
	FirstName VARCHAR(50),
	LastName VARCHAR(50),
	Email Varchar(255),
	Course VARCHAR(50)
);

CREATE TABLE course (
	id INT PRIMARY KEY,
	CourseID INT(50),
	CourseName VARCHAR(50)
);

INSERT INTO students(StudentID, firstName, course, email)
VALUES ('2', 'John Doe', 'BSIS3B', 'sample@gmail.com');

UPDATE students SET course = 'ACT2'
WHERE studentID = 2;

DELETE FROM students
WHERE email = "sample@gmail.com";